# Where we stopped — 31 August 2026

Live: https://williamperkins14.github.io/conflict-globe/

## Done today

- detect-locations.mjs was silently wiping all 130 Telegram events on every
  6-hour scheduled run. Fixed, regression-tested, and PROVEN in production —
  the scheduled job ran again at 08a1891 and the events survived.
- GDELT diagnosed: it works from GitHub runners but takes 23-24s per query.
  Our abort was at 20s, so every good query died 3s early. That was the whole
  reason the first ladder run promoted nothing. Timeout now 60s with retries.
- The browser's news panel had the same bug (22s abort). Now 45s.
- Adversarial corroboration: promotion needs OPPOSING sides to agree, or a
  third-party wire. Two Ukrainian channels agreeing is one perspective twice.
  source-tiers.json holds the classification and is William's to edit.
- Leningrad-class fix: "<Name> region" no longer falls back to a same-named
  settlement inside the bbox. It was putting Leningrad oblast on a village in
  Kherson, 1,400km out and in the wrong country.
- Every key event now carries the source it was checked against: 69 of 70
  cited. The one uncited row says "(no citation)" rather than hiding it.
- Sudan drafted: 5 write-ups, conflict summary, 14 key events, all cited.
- Junk markers Serhiyi (a person's name) and Leningrad removed.

## THE ONE THING TO DO NEXT

Re-run the ladder: Actions -> "Backfill Telegram events" -> Run workflow ->
choose `ladder`.

It runs on GitHub. It does NOT need William's computer on. Trigger and walk
away; it commits its own results.

The first run was killed at a 60-minute step limit having written nothing —
every result was held in memory until one write at the end. Now fixed: it
flushes every 5 place+date groups and skips events an earlier run already
checked (`checkedOn`), so it resumes and a kill is survivable. Step timeout
raised 60 -> 335 minutes.

Expect it to take over an hour. 92 queries x 24s, plus retries on the ~50%
of connections that fail first time.

## What the run will tell us

Whether ANY event can climb the ladder. Right now: 130 events, all
`reported`, zero corroborated. Both Telegram channels are Ukrainian-aligned,
so every promotion must come from a Russian outlet or a wire.

If it promotes very little, that is a finding rather than a bug: two
overlapping channels are too narrow a base. The fix is a third channel on
the Russian side, or ACLED.

## Still open

- wartranslated has NEVER been backfilled. noel_reports has 3,088 posts back
  to 12 June; the second channel has zero. Biggest single improvement left,
  and it roughly doubles the source pool. Run the `backfill` job for it.
- Gaza, Haiti and Iran have no write-ups and no key events. The generator
  now handles multiple conflicts — each needs two markdown files.
- Dobropillia is filed under Pokrovsk (20km out, arguably the right sector,
  but the sentence names a different town). William's call.
- ACLED / UCDP emails still unsent.
- DATA.md not written: schema, sources, coverage window, known gaps.

## Working rule learned the hard way

Do not let Claude Code and Cowork edit the same working tree at once. It
cost a commit that missed a change, and a write that nearly deleted 1,547
lines. One holds the pen; the other reads.
